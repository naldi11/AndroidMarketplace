<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_logo_market_mahasiswa"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo_market_mahasiswa"
        android:supportsRtl="true"
        android:theme="@style/Theme.MarketPlace">

        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.auth.LoginActivity" />
        
        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.product.ProductDetailActivity" />
        <activity android:name=".ui.product.AddProductActivity" />
        <activity android:name=".ui.product.MyProductsActivity" />
        <activity android:name=".ui.product.EditProductActivity" />
        <activity android:name=".ui.seller.MyOrdersActivity" />
        <activity android:name=".ui.seller.SellerOrdersActivity" />
        <activity android:name=".ui.seller.SellerDashboardActivity" />
        <activity android:name=".ui.seller.SellerBalanceActivity" />
        <activity android:name=".ui.cart.CartActivity" />
        <activity android:name=".ui.cart.CheckoutActivity" />
        <activity android:name=".ui.transaction.TransactionActivity" />
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity android:name=".ui.profile.WishlistActivity" />
        <activity android:name=".ui.profile.AddressActivity" />
        <activity android:name=".ui.profile.MapPickerActivity" />
        <activity android:name=".ui.profile.ManageAddressActivity" />
        <activity android:name=".ui.profile.AddEditAddressActivity" />
        <activity android:name=".ui.seller.OrderDetailActivity" />


        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".ui.payment.PaymentActivity"
            android:label="Pembayaran" />

    </application>

</manifest>